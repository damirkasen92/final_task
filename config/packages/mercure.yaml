mercure:
    hubs:
        default:
            url: "%env(MERCURE_URI)%"
            public_url: "%env(MERCURE_PUBLIC_URI)%"
            jwt:
                secret: "%env(MERCURE_JWT_SECRET)%"
                algorithm: "hmac.sha256"
                publish: ["*"]
                subscribe: ["*"]
